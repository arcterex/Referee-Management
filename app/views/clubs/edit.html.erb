<h1>Editing club</h1>

<% form_for(@club) do |f| %>
  <%= render :partial => "edit_club", :locals => { :f => f } %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @club %> |
<%= link_to 'Back', clubs_path %>
