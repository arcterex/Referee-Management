<h1>Editing admin</h1>

<% form_for(@admin) do |f| %>
  <%= render :partial => "edit_admin", :locals => { :f => f } %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @admin %> |
<%= link_to 'Back', admins_path %>
